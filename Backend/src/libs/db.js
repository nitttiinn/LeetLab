/*
 1. PrismaClient is a class that is used to interact with the database.
 2. It is generated by Prisma based on the schema defined in the Prisma schema file.
*/
// importing the PrismaClient from the generated Prisma client
import {PrismaClient} from '../generated/prisma/index.js';  
// this is a workaround to avoid the multiple instances of PrismaClinet in development mode

const globalForPrisma = globalThis;

export const db = globalForPrisma.prisma || new PrismaClient();

if(process.env.NODE_ENV !== 'production') globalForPrisma.prisma = db;